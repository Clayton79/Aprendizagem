const repetir = function(laco, num) {
        let resultado = []
        console.log(laco);
        for(let i = 0; i < num; i++) {
        resultado.push(laco); 
        }
        console.log(resultado);
}

repetir("código", 2);
repetir(7, 3);